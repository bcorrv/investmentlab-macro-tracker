name: Daily Macro Update

on:
  schedule:
    - cron: "05 12 * * *"   # 09:05 Chile aprox (UTC-3). GitHub usa UTC
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Debug list files
        run: |
          pwd
          ls -la
          echo "----"
          ls -la requirements.txt || true

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Pull existing historical data from data repo
        env:
          TOKEN: ${{ secrets.DATA_REPO_TOKEN }}
          OWNER: bcorrv
          DATA_REPO: investmentlab-data
        run: |
          git clone https://$TOKEN@github.com/$OWNER/$DATA_REPO.git out
          mkdir -p data
          cp -f out/*.csv data/ 2>/dev/null || true

      - name: Run pipeline
        run: |
          python main.py

      - name: Publish updated data back to data repo
        env:
          TOKEN: ${{ secrets.DATA_REPO_TOKEN }}
          OWNER: bcorrv
          DATA_REPO: investmentlab-data
        run: |
          cp -f data/*.csv out/ 2>/dev/null || true
          cd out
          git config user.email "actions@github.com"
          git config user.name "github-actions"
          git add *.csv || true
          git commit -m "Update macro data" || echo "No changes"
          git push

      - name: Publish snapshot to data repo
        env:
          TOKEN: ${{ secrets.DATA_REPO_TOKEN }}
          OWNER: bcorrv
          DATA_REPO: investmentlab-data
        run: |
          git clone https://$TOKEN@github.com/$OWNER/$DATA_REPO.git out
          cp data/daily_snapshot.csv out/daily_snapshot.csv
          cp data/run_summary.json out/run_summary.json
          cd out
          git config user.email "actions@github.com"
          git config user.name "github-actions"
          git add daily_snapshot.csv run_summary.json
          git commit -m "Update daily snapshot + summary" || echo "No changes"
          git push

